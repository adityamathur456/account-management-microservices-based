server:
  port: 8888

spring:
  application:
    name: config-service

  profiles:
    active: native   # ✅ tells it to use local files

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config   # or file:/absolute/path/to/config
